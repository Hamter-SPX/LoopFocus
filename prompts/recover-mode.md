# Recover Mode — dispatch prompt

Load the LoopFocus skill first, then enter recover mode.

Order (exactly — flow/recovery-flow.md):
1. READ .loopfocus/state.md FIRST (goal, DONE, PROVEN, UNKNOWN, NEXT)
2. READ ledger + query the genome for the problem class
3. CROSS-CHECK state against reality (git status, re-run last evidence)
4. state-check — the capsule must be machine-readable
5. Resume at NEXT — do NOT redo PROVEN work
6. Record: resumed from checkpoint <hash> | verified: <cross-checks>

Return: what was resumed, what was re-verified, and the new NEXT.
