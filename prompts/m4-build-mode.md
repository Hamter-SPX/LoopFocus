# M4 Build Mode — dispatch prompt

Load the LoopFocus skill first, then enter M4 Build Mode (announce it).

Task: <describe the feature — requirement, constraints, acceptance>

Discipline required (references/build-mode.md, flow/feature-build-flow.md):
1. LOCK: restate the requirement in your own words (Intent Anchor). Lock goal + invariants + profile.
2. Canvas the architecture (where it plugs in, edges, invariants). Get approval on non-trivial structures.
3. Predictive pass: touch map + risk factors + confidence levels (Known / Likely / Unknown).
4. DoD graph into .loopfocus/dod.md before the first edit.
5. First thinnest end-to-end slice → verify → commit. Then slice-by-slice loops.
6. Scope firewall: new ideas go to the SkillFocus report, not the code, until approved.
7. Finish: full gate chain + verify script + genome record + completion report with verification gaps.

Return the completion report in the LoopFocus order (SKILL.md Completion Report Contract).
